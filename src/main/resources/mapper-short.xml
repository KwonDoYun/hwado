<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-short">

	<!-- 전체 목록 조회 -->
	<select id="getShortList" resultType="lx.sia.community.vo.ShortVO">
		select *
		from line_board
		order by board_id desc
	</select>
	
	<!-- 특정 해시태그 목록 조회 -->
	<select id="getListByHashTag" parameterType="String" resultType="lx.sia.community.vo.ShortVO">
		select *
		from line_board
		where hashtag = #{hashtag}
		order by board_id desc
	</select>
	
	<!-- 어떤 해시태그들이 있는지 조회 -->
	<select id="getHashtag" resultType="String">
		select hashtag
		from line_board
		group by hashtag
	</select>
	
	<!-- 등록하기 -->
	<insert id="registerShortBoard" parameterType="lx.sia.community.vo.ShortVO" useGeneratedKeys="true" keyColumn="board_id" keyProperty="boardId">
		insert into line_board(content, hashtag, user_id, reg_date)
		values (#{content}, #{hashtag}, #{userId}, DATE_ADD(NOW(), INTERVAL 9 HOUR))
	</insert>
	
	
	<!-- 수정하기 -->
	<update id="updateShortBoard" parameterType="lx.sia.community.vo.ShortVO" useGeneratedKeys="true" keyColumn="board_id" keyProperty="boardId">
		update line_board
		set content = #{content},
			hashtag = #{hashtag}
		where board_id = #{boardId}
	</update>
	
	<!-- 삭제하기 -->
	<delete id="deleteShortBoard" parameterType="int">
		delete from line_board
		where board_id = #{boardId}
	</delete>

</mapper>