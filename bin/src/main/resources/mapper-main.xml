<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-main">
	<select id="getUserCount" resultType="int">
		SELECT count(*) FROM users
	</select>
	
	<select id="getNewUserCount" resultType="int">
		SELECT count(*) FROM users 
		WHERE date(reg_date)= date(DATE_ADD(NOW(), INTERVAL 9 HOUR))
	</select>
	
	<select id="getLatestGenralBoard" resultType="lx.sia.community.vo.BoardVO">
		SELECT @ROWNUM := @ROWNUM + 1 AS ROWNUM, A.* FROM
        (SELECT * FROM general_board ORDER BY board_id DESC) A, 
        (SELECT @ROWNUM := 0 ) B
	</select>
	
	<select id="getLatestNotice" resultType="lx.sia.community.vo.NoticeVO">
		SELECT @ROWNUM := @ROWNUM + 1 AS ROWNUM, A.* FROM
        (SELECT * FROM notice_board ORDER BY board_id DESC) A, 
        (SELECT @ROWNUM := 0 ) B
	</select>
</mapper>